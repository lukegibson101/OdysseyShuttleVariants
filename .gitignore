# RimWorld mod build output (DLL is a build artifact — never commit; the standalone
# Harmony mod / Prepatcher provides the runtime 0Harmony.dll)
Assemblies/*.dll
Assemblies/*.pdb
[Oo]bj/
[Bb]in/
*.csproj.user
.vs/
.idea/

# Mod art IS tracked: custom AI-generated sprites plus a few vanilla bases used as permitted by
# Ludeon's modding terms (disclaimer ships in About.xml). Only the build output above is ignored.

# OS cruft
Thumbs.db
.DS_Store
